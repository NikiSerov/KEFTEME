export const LS_TOKEN_KEY = "keftemeToken";

type ModalTypes = {
  success: string;
  error: string;
}

export const modalTypes: ModalTypes = {
  success: "success",
  error: "error",
};

export const defaultLimit = 8;
